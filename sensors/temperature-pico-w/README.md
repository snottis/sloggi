# Temperature Sensor on Pico W

Uses Raspberry Pi Pico W board with MicroPython to collect and send temperature data to **sloggi** backend.

## Installing requirements

```
pip install -r requirements.txt -t lib/
```